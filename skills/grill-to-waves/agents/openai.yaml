# Codex CLI skill metadata. Claude Code ignores this file; it reads only SKILL.md.
# `allow_implicit_invocation: false` is the Codex equivalent of SKILL.md's
# `disable-model-invocation: true`: the skill runs only when the user types `$grill-to-waves`.
interface:
  display_name: "Grill to Waves"
  short_description: "Plan specs, tickets, waves and an execution map"
  default_prompt: "Use $grill-to-waves to turn this idea into a verified execution map."

policy:
  allow_implicit_invocation: false
